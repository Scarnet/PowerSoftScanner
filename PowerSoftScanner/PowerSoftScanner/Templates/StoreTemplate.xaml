<?xml version="1.0" encoding="UTF-8"?>
<DataTemplate xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PowerSoftScanner.Templates.StoreTemplate">
    <ViewCell>
        <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand" Margin="10">
            <Label Style="{StaticResource ListLabel}" Text="{Binding Name}" />
            <Label Style="{StaticResource ListLabel}" HorizontalOptions="EndAndExpand" Text="{Binding Code}" />
        </StackLayout>
    </ViewCell>
</DataTemplate>